#!/bin/bash

set -ex

jupyter nbextension enable --py widgetsnbextension --sys-prefix

git clone https://github.com/PBrockmann/fast

export FER_DATA=". ${FER_DATA} ~/fast"
export FER_GO=". ${FER_GO} ~/fast"
export FER_PALETTE=". /${FER_PALETTE} ~/fast"
export PATH="${FER_DIR}/bin:~/fast:${PATH}"
